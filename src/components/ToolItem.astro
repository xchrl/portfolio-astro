---
import { items } from "@/data/aboutItems.ts";
import { Image } from "astro:assets";

const { type, name } = Astro.props;

const item = items[type][name];
const link = item.href;
const icon = item.icon;
---

<a
  class={`relative px-4 py-2 border border-accent rounded-md flex items-center gap-4 hover:scale-125 hover:bg-accent hover:text-background hover:z-1 duration-150 hover:cursor-pointer ${item.highlight ? "bg-accent/20" : ""}`}
  href={link}
  target="_blank"
>
  {
    name === "The NOexistenceN of you AND me" && (
      <>
        <span class="text-lg absolute -top-3 left-4">💖</span>
        <span class="text-lg absolute -right-2">💖</span>
        <span class="text-lg absolute -bottom-3 left-1/2">💖</span>
      </>
    )
  }
  {icon && <Image src={icon} format="svg" alt={name + " icon"} width="16" />}
  <span class="font-bold">{name}</span>
</a>
